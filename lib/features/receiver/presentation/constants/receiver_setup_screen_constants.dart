import 'package:flutter/material.dart';

/// Receiver Setup Screen의 상수 값들을 정의합니다.
class ReceiverSetupScreenConstants {
  ReceiverSetupScreenConstants._();

  // 윈도우 스타일 상수
  static const Size setupWindowSize = Size(800, 600);
  static const double windowOpacity = 1.0;

  // UI 레이아웃 상수
  static const double qrCodeSize = 200.0;
  static const double qrCodeSpacing = 20.0;
  static const double statusSpacing = 30.0;
  static const double statusTextSpacing = 10.0;
  static const double monitorSelectorPadding = 32.0;
  static const double monitorSelectorSpacing = 20.0;
  static const double verticalDividerWidth = 1.0;

  // 오버레이 설정 상수
  static const Duration overlayDelay = Duration(milliseconds: 100);
}
